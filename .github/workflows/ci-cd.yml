name: CI/CD - Mediplus

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  IMAGE_NAME: mediplus
  IMAGE_TAG: ${{ github.run_number }}
  ECR_REGISTRY: 503640389215.dkr.ecr.eu-north-1.amazonaws.com/mediplus
  REGION: eu-north-1
  K8S_NAMESPACE: default
  DEPLOYMENT_NAME: mediplus-deployment
  GITOPS_REPO: https://github.com/Christianchika/kubernetes-manifest.git

jobs:
  build-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.REGION }}

      - name: Login to ECR
        run: |
          aws ecr get-login-password --region $REGION | docker login --username AWS --password-stdin $ECR_REGISTRY

      - name: Build Docker Image
        run: docker build -t $IMAGE_NAME:${IMAGE_TAG} .

      - name: Tag Docker Image
        run: docker tag $IMAGE_NAME:${IMAGE_TAG} $ECR_REGISTRY:${IMAGE_TAG}

      - name: Push Docker Image to ECR
        run: docker push $ECR_REGISTRY:${IMAGE_TAG}

  update-deployment:
    runs-on: ubuntu-latest
    needs: build-push
    steps:
      - name: Clone kubernetes-manifest repo
        env:
          GIT_USERNAME: ${{ secrets.GIT_USERNAME }}
          GIT_PASSWORD: ${{ secrets.GIT_PASSWORD }}
        run: |
          git clone https://${GIT_USERNAME}:${GIT_PASSWORD}@github.com/Christianchika/kubernetes-manifest.git
          cd kubernetes-manifest

          echo "✅ Before update:"
          cat deployment.yaml

          # Update image tag in deployment.yaml
          sed -i "s|503640389215.dkr.ecr.eu-north-1.amazonaws.com/mediplus:.*|503640389215.dkr.ecr.eu-north-1.amazonaws.com/mediplus:${IMAGE_TAG}|g" deployment.yaml

          echo "✅ After update:"
          cat deployment.yaml

          git config user.email "okoro.christianpeace@gmail.com"
          git config user.name "${GIT_USERNAME}"
          git add deployment.yaml
          git commit -m "Update Mediplus image to ${IMAGE_TAG}" || echo "No changes to commit"
          git push https://${GIT_USERNAME}:${GIT_PASSWORD}@github.com/Christianchika/kubernetes-manifest.git HEAD:main

  verify-rollout:
    runs-on: ubuntu-latest
    needs: update-deployment
    steps:
      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'v1.30.0'

      - name: Configure kubeconfig
        run: |
          aws eks update-kubeconfig --region $REGION --name mediplus

      - name: Verify Deployment Rollout
        run: |
          kubectl rollout status deployment/${DEPLOYMENT_NAME} -n ${K8S_NAMESPACE}


























# name: Build and Push to ECR

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-push:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Login to Amazon ECR
#         run: |
#           aws ecr get-login-password --region eu-north-1 | docker login --username AWS --password-stdin 892004491300.dkr.ecr.eu-north-1.amazonaws.com

#       - name: Build, tag, and push image to Amazon ECR
#         run: |
#           docker build -t mediplus:v1 .
#           docker tag mediplus:v1 892004491300.dkr.ecr.eu-north-1.amazonaws.com/mediplus:v1
#           docker push 892004491300.dkr.ecr.eu-north-1.amazonaws.com/mediplus:v1

